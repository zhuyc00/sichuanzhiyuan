<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shichuan.zhiyuan.mappers.NewsCategoriesMappers">
	<!--实体映射-->
	<resultMap id="base_result_map" type="shichuan.zhiyuan.entity.po.NewsCategories">
		<!--  -->
		<id column="id" property="id"/>
		<!--  -->
		<result column="name" property="name"/>
		<!--  -->
		<result column="description" property="description"/>
		<!--  -->
		<result column="created_at" property="createdAt"/>

	</resultMap>

	<!--通用查询列-->
	<sql id="base_column_list">
		id,name,description,created_at
	</sql>

	<!--基础查询条件-->
	<sql id="base_query_condition">

		<if test="query.id!=null">
			and id = #{query.id}
		</if>
		<if test="query.name!=null and query.name!=''">
			and name = #{query.name}
		</if>
		<if test="query.description!=null and query.description!=''">
			and description = #{query.description}
		</if>
		<if test="query.createdAt!=null">
			and created_at = #{query.createdAt}
		</if>
	</sql>

	<!--拓展的查询条件-->
	<sql id="base_query_condition_extend">
		<if test="query.nameFuzzy!=null and query.nameFuzzy!=''">
			and name like concat ('%', #{query.nameFuzzy}, '%')
		</if>
		<if test="query.descriptionFuzzy!=null and query.descriptionFuzzy!=''">
			and description like concat ('%', #{query.descriptionFuzzy}, '%')
		</if>
		<if test="query.createdAtStart!=null and query.createdAtStart!=''">
			<![CDATA[ and created_at>=str_to_date(#{query.createdAtStart}, '%Y-%m-%d') ]]>
		</if>
		<if test="query.createdAtEnd!=null and query.createdAtEnd!=''">
			<![CDATA[ and created_at< date_sub(str_to_date(#{query.createdAtEnd},'%Y-%m-%d')，interval -1 day) ]]>
		</if>

	</sql>

	<!--通用的查询条件-->
	<sql id="query_condition">
		<where>
			<include refid="base_query_condition"/>
			<include refid="base_query_condition_extend"/>
		</where>
	</sql>

	<!--查询列表-->
	<select id = "selectList" resultMap="base_result_map">
		SELECT
		<include refid="base_column_list"/>
		FROM news_categories
		<include refid="query_condition"/>
		<if test="query.orderBy!=null">order by ${query.orderBy}</if>
		<if test="query.simplePage!=null">limit #{query.simplePage.start},#{query.simplePage.end}</if>
	</select>

	<!--查询数量-->
	<select id = "selectCount" resultType="java.lang.Integer">
		SELECT COUNT(1) FROM news_categories
		<include refid="query_condition"/>
	</select>

	<!--插入 （匹配有值的字段）-->
	<insert id = "insert" parameterType="shichuan.zhiyuan.entity.po.NewsCategories">
		<selectKey keyProperty="bean.id" resultType="Integer" order="AFTER">
			SELECT LAST_INSERT_ID()
		</selectKey>
		INSERT INTO news_categories
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="bean.id != null">
				id,
			</if>
			<if test="bean.name != null">
				name,
			</if>
			<if test="bean.description != null">
				description,
			</if>
			<if test="bean.createdAt != null">
				created_at,
			</if>
		</trim>
		<trim prefix="values (" suffix=")" suffixOverrides=",">
			<if test="bean.id != null">
				#{bean.id},
			</if>
			<if test="bean.name != null">
				#{bean.name},
			</if>
			<if test="bean.description != null">
				#{bean.description},
			</if>
			<if test="bean.createdAt != null">
				#{bean.createdAt},
			</if>
		</trim>
	</insert>

		<!-- 插入或者更新 （匹配有值的字段） -->
	<insert id = "insertOrUpdate" parameterType="shichuan.zhiyuan.entity.po.NewsCategories">
		INSERT INTO news_categories
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="bean.id != null">
				id,
			</if>
			<if test="bean.name != null">
				name,
			</if>
			<if test="bean.description != null">
				description,
			</if>
			<if test="bean.createdAt != null">
				created_at,
			</if>
		</trim>
		<trim prefix="values (" suffix=")" suffixOverrides=",">
			<if test="bean.id != null">
				#{bean.id},
			</if>
			<if test="bean.name != null">
				#{bean.name},
			</if>
			<if test="bean.description != null">
				#{bean.description},
			</if>
			<if test="bean.createdAt != null">
				#{bean.createdAt},
			</if>
		</trim>
		on DUPLICATE key update
		<trim prefix="" suffix="" suffixOverrides=",">
			<if test="bean.name != null">
				 name = VALUES(name)
			</if>
			<if test="bean.description != null">
				 description = VALUES(description)
			</if>
			<if test="bean.createdAt != null">
				 created_at = VALUES(created_at)
			</if>
		</trim>
	</insert>

	<!-- 批量插入 -->
	<insert id ="insertBatch" parameterType="shichuan.zhiyuan.entity.po.NewsCategories">
		INSERT INTO news_categories(name,description,created_at) VALUES
		<foreach collection="list" item="item" separator=",">
			(#{item.name},#{item.description},#{item.createdAt})
		</foreach>
	</insert>

	<!-- 批量插入（或者更新） -->
	<insert id ="insertOrUpdateBatch" parameterType="shichuan.zhiyuan.entity.po.NewsCategories">
		INSERT INTO news_categories(name,description,created_at) VALUES
		<foreach collection="list" item="item" separator="," >
			(#{item.name},#{item.description},#{item.createdAt})
		</foreach>
			on DUPLICATE key update
			name= VALUES(name),description= VALUES(description),created_at= VALUES(created_at)
	</insert>

	<!-- 根据Id查询 -->
	<select id="selectById" resultMap="base_result_map">
		select <include refid="base_column_list"/> from news_categories where id=#{id}
	</select>

	<!-- 根据Id更新 -->
	<update id="updateById" parameterType="shichuan.zhiyuan.entity.po.NewsCategories">
		update news_categories
		<set>
			<if test="bean.id != null">
				id = #{bean.id},
			</if>
			<if test="bean.name != null">
				name = #{bean.name},
			</if>
			<if test="bean.description != null">
				description = #{bean.description},
			</if>
			<if test="bean.createdAt != null">
				created_at = #{bean.createdAt},
			</if>
		</set>
		where id=#{id}
	</update>

	<!-- 根据Id删除 -->
	<delete id="deleteById">
		delete from news_categories where id=#{id}
	</delete>
</mapper>